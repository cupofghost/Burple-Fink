# STATUS

Last consolidation: (never)

## Active work
| Date | Area / files | Task & state (≤3 lines) | Signature |
|------|--------------|-------------------------|-----------|
| 2026-07-24 | data/ | ✅ WS-1 batch 1: Added 3 datasets (racehorses 355, spacecraft 270, paint_colors 391). Smoke-tested; registered. | Signed: Claude Code \| Haiku 4.5 \| low |
| 2026-07-24 | data/ | ✅ WS-1 batch 2: Added 3 datasets (motorcycles 359, craft_beers 398, aircraft 435). All ≥300 entries. Smoke-tested; registered. | Signed: Claude Code \| Haiku 4.5 \| low |
| 2026-07-24 | web/, src/serve.py, src/export_web.py, src/finetune.py, data/ | ✅ WS-1+5: engine selector -> dropdown; "compare engines" modal (dataset size + WS-3 plausibility as "strength"); added 5 datasets (team_names 427, dog_breeds 378, food_brands 565, firearm_names 560, band_names 988); rebuilt base (hidden_dim 96, was 256 — 13-model export was 92MB at 256, ~14MB at 96, no quality loss) + all 13 fine-tunes; re-exported web/burple-fink.html (13 engines, ~14MB). | Signed: Claude Code \| Sonnet 5 \| high |

## Shared-file touches
- `data/shared_vocab.json` rebuilt (deleted + regenerated by `src.pretrain`) to add apostrophe/ampersand/period/digit characters needed by the 5 new datasets. No checkpoints existed locally before this session, so nothing depended on the old vocab's exact indices. 2026-07-24. Signed: Claude Code | Sonnet 5 | high
- `src/finetune.py` defaults raised (`FINETUNE_EPOCHS` 60->150, `FINETUNE_LR` 5e-4->2e-3): with 13 diverse datasets sharing one base, the old gentler defaults left visible cross-domain leakage in samples (e.g. dog breeds with beer/aircraft fragments). New defaults verified clean across all current datasets; still overridable via `--epochs`/`--lr`. 2026-07-24. Signed: Claude Code | Sonnet 5 | high

## Known issues
- `data/paint_colors.txt` lines ~275-391 are corrupted/gibberish (e.g. "Bemidgard", "Bemidillion") — looks like leftover char-RNN sample output pasted in instead of real color names, not real paint colors. Not touched (outside this task's scope). 2026-07-24. Signed: Claude Code | Sonnet 5 | high

## Archive
(completed entries moved here during consolidation — one line each)
